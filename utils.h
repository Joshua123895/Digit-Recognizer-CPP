#ifndef UTILS_H
#define UTILS_H

// Function to move cursor for progress bar
void moveCursorTo(int x, int y);

// Function to change terminal color
void change_colour(int a);

void reset_colour();

void fullscreen();

#endif
